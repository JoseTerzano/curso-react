

const activo = true;
/*
let mensaje = '';

if(!activo){
    mensaje = 'activo';
}else{
    mensaje = 'Inactivo'
}
*/
//const mensaje = (activo) ? 'Activo' : 'Inactivo';
//const mensaje = (!activo) ? 'Activo' : null;
const mensaje = activo && 'Activo' // para cuando queramos que se cumpla solo una condicion



console.log(mensaje);

